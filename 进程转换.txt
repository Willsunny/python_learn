就绪态:等待cpu调度的进程的状态。  
运行状态：cpu调度的进程处于运行状态。
不可中断状态：运行状态的进程需要申请I/0或者内存资源的时候就会进入不可中断状态。等到申请的资源有效之后就会转换成就绪状态。
可中断状态：等待申请资源有效或者收到信号之后转换成就绪状态的进程。
退出状态：当一个进程运行结束之后就会成为退出状态。x：即将被销毁的进程。z:僵尸进程，保留了少量资源的进程。
暂停状态：人为的暂停某个进程，内核不再调用该进程。



